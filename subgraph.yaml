specVersion: 1.0.0
indexerHints:
  prune: auto
schema:
  file: ./schema.graphql
dataSources:
  - kind: ethereum
    name: TokenFactory
    network: chapel
    source:
      address: "0x3f935af2baEff35851B0a8E635ee0aB7e22E9d94"
      abi: TokenFactory
      startBlock: 44589274
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.7
      language: wasm/assemblyscript
      entities:
        - Token
        - TransferEvent
        - Account
        - AccountBalance
      abis:
        - name: TokenFactory
          file: ./abis/TokenFactory.json
      eventHandlers:
        - event: CreatedMemeToken(indexed address,indexed address,string,string)
          handler: handleCreatedMemeToken
        - event: BoughtMemeToken(indexed address,indexed address,uint256)
          handler: handleBoughtMemeToken
        - event: BoughtCrosschainMemeToken(indexed uint32,indexed address,indexed address,uint256)
          handler: handleBoughtCrosschainMemeToken
        - event: SoldMemeToken(indexed address,indexed address,uint256)
          handler: handleSoldMemeToken
        - event: SoldCrosschainMemeToken(indexed uint32,indexed address,indexed address,uint256)
          handler: handleSoldCrosschainMemeToken
      file: ./src/token-factory.ts
templates:
  # These data sources templates exist to support the different types that a specific erc20 token could be.
  - name: StandardToken
    kind: ethereum/contract
    network: chapel
    source:
      abi: ERC20
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.7
      language: wasm/assemblyscript
      file: ./src/token.ts
      entities:
        - TransferEvent
      abis:
        - name: ERC20
          file: ./abis/ERC20.json
      eventHandlers:
        - event: Transfer(indexed address,indexed address,uint256)
          handler: handleTransfer
